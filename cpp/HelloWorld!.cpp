#include<iostream>

// the part include will include a specific library

/*
    we start by adding the main function and use std or standard with
    cout or character out to print out a command. we can use endl with std to
    end line there or use /n with these '' to do the same and comment in what you are
    reading dumbass
*/
int main(){

    std::cout << "Hello world!" << std::endl;
    std::cout << "Now bye world!" << '\n';

    return 0;
}